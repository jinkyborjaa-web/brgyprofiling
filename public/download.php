<?php require_once __DIR__.'/includes/bootstrap.php';$id=(int)($_GET['id']??0);$doc=rows($pdo,'SELECT title,file_path FROM documents WHERE id=? LIMIT 1',[$id])[0]??null;$path=$doc?realpath($doc['file_path']??''):false;if(!$doc||!$path||!is_file($path)){http_response_code(404);exit('Document not found.');}header('Content-Type: application/pdf');header('Content-Disposition: attachment; filename="'.preg_replace('/[^a-z0-9._-]/i','_',($doc['title']??'document')).'.pdf"');readfile($path);exit;
