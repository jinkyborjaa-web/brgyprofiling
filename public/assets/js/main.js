function openGate(intent){const modal=document.getElementById('login-gate');if(!modal)return;document.getElementById('gate-message').textContent='Please sign in to '+(intent||'continue')+'.';const link=document.getElementById('gate-link');link.href='login.php?'+new URLSearchParams({redirect:location.pathname+location.search+location.hash,intent:intent||'continue'});modal.hidden=false}function closeGate(){const modal=document.getElementById('login-gate');if(modal)modal.hidden=true}document.querySelectorAll('[data-requires-login]').forEach(el=>el.addEventListener('click',e=>{if(el.dataset.authenticated!=='1'){e.preventDefault();openGate(el.dataset.intent)}}));
